# 2026-06-22 개발 로그

## 오늘 한 일

- 폴더 구조 자동화 프로그램의 초기 프로젝트 구조를 구성했다.
- `Input` 폴더의 `.md`, `.txt`, `.docx`, `.hwpx` 입력 파일을 읽어 바탕화면에 폴더 구조를 생성하는 실행 흐름을 마련했다.
- 명시적인 디렉토리 트리가 있는 입력은 해당 구조를 우선 사용하고, 트리가 없는 보고서형 문서는 제목/번호/스타일 기반 구조로 해석하도록 기능을 분리했다.
- 핵심 실행 진입점 `logic/folder_builder.ps1`와 Windows 실행용 `run_folder_builder.cmd`를 구성했다.
- 입력 처리, 경로명 정규화, 인라인 주석 제거, 중복 루트 제거, 문서별 레코드 읽기 기능을 `logic/functions` 아래에 함수 단위로 분리했다.
- 보고서형 문서에서 개발 책임 영역과 기본 산출물 구조를 추출하는 `logic/descript` 책임 영역을 추가했다.
- README에 사용 방법, 지원 확장자, dry-run 실행, 동작 규칙을 정리했다.
- Git 텍스트 정규화를 위해 `.gitattributes`를 추가했다.
- 로컬 실행 중 생성되는 `debug.log`를 버전 관리 대상에서 제외하도록 `.gitignore`에 등록했다.

## 확인한 상태

- 오늘 커밋: `aed3700 Initial commit`
- 커밋 기준 변경 규모: 29개 파일, 1063줄 추가
- 현재 `debug.log`는 작업 로그 파일로 확인되며, 저장소에 추적되지 않는 상태였다.
- `develop logs` 폴더에는 기존 날짜 로그가 없어 오늘 날짜 파일을 새로 작성했다.

## 오늘 검증

- `git check-ignore -v debug.log`로 `debug.log`가 `.gitignore` 규칙에 의해 제외되는 것을 확인했다.
- `git status --short --ignored`에서 `debug.log`가 `!! debug.log`로 표시되는 것을 확인했다.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\logic\folder_builder.ps1 -DryRun`을 실행해 입력 파일이 없을 때 정상 안내 메시지로 종료되는 것을 확인했다.
- `logic/descript` 로드 후 샘플 보고서 레코드로 `Test-DescriptReport`가 `true`를 반환하는 것을 확인했다.

## 다음 확인 필요

- 실제 샘플 입력 파일을 `Input`에 넣고 정상 생성 흐름을 검증해야 한다.
- `.docx`, `.hwpx` 파서는 실제 문서 샘플로 경계값을 확인해야 한다.
- README 일부 한글이 현재 콘솔에서 깨져 보이므로 파일 인코딩 표시/렌더링 상태를 별도로 확인할 필요가 있다.
